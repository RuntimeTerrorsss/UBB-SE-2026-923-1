@using BookingBoardGames.Data.Enum
@model BookingBoardGames.Sharing.DTO.ConversationDTO

<div class="d-flex flex-column h-100 w-100">

    <div class="p-3 border-bottom bg-white d-flex align-items-center shadow-sm z-1 flex-shrink-0">
        <div class="rounded-circle bg-secondary d-flex justify-content-center align-items-center text-white me-3 flex-shrink-0" style="width: 40px; height: 40px;">
            <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" viewBox="0 0 16 16">
                <path d="M3 14s-1 0-1-1 1-4 6-4 6 3 6 4-1 1-1 1zm5-6a3 3 0 1 0 0-6 3 3 0 0 0 0 6" />
            </svg>
        </div>
        <h5 class="mb-0 fw-bold">@ViewBag.OtherUserName</h5>
    </div>

    <div class="flex-grow-1 p-4 overflow-auto" id="chatScrollArea" style="background-color: #f3f2f1;">
        @if (Model.MessageList == null || !Model.MessageList.Any())
        {
            <div class="text-center text-muted mt-4"><small>No messages yet. Say hi!</small></div>
        }
        else
        {
            foreach (var msg in Model.MessageList)
            {
                if (msg.Type == MessageType.MessageSystem)
                {
                    <div class="text-center my-3">
                        <span class="badge bg-secondary rounded-pill py-2 px-3 fw-normal shadow-sm" style="opacity: 0.85;">
                            @msg.Content
                        </span>
                    </div>
                    continue;
                }

                bool isMine = msg.SenderId == ViewBag.CurrentUserId;

                <div class="d-flex mb-3 @(isMine ? "justify-content-end" : "justify-content-start")">
                    <div class="p-2 rounded-3 shadow-sm" style="max-width: 75%; background-color: @(isMine ? "#0078d4" : "#ffffff"); color: @(isMine ? "#ffffff" : "#242424");">

                        @if (msg.Type == MessageType.MessageRentalRequest)
                        {
                            <div class="d-flex align-items-center mb-2 pb-2 border-bottom @(isMine ? "border-light" : "border-secondary")" style="opacity: 0.9;">
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="me-2" viewBox="0 0 16 16">
                                    <path d="M3.5 0a.5.5 0 0 1 .5.5V1h8V.5a.5.5 0 0 1 1 0V1h1a2 2 0 0 1 2 2v11a2 2 0 0 1-2 2H2a2 2 0 0 1-2-2V3a2 2 0 0 1 2-2h1V.5a.5.5 0 0 1 .5-.5M1 4v10a1 1 0 0 0 1 1h12a1 1 0 0 0 1-1V4z" />
                                </svg>
                                <strong>Rental Request</strong>
                            </div>
                        }

                        @if (msg.Type == MessageType.MessageCashAgreement)
                        {
                            <div class="d-flex align-items-center mb-2 pb-2 border-bottom @(isMine ? "border-light" : "border-secondary")" style="opacity: 0.9;">
                                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="me-2" viewBox="0 0 16 16">
                                    <path d="M8 10a2 2 0 1 0 0-4 2 2 0 0 0 0 4" />
                                    <path d="M0 4a1 1 0 0 1 1-1h14a1 1 0 0 1 1 1v8a1 1 0 0 1-1 1H1a1 1 0 0 1-1-1zm3 0a2 2 0 0 1-2 2v4a2 2 0 0 1 2 2h10a2 2 0 0 1 2-2V6a2 2 0 0 1-2-2z" />
                                </svg>
                                <strong>Cash Agreement</strong>
                            </div>
                        }

                        @if (!string.IsNullOrEmpty(msg.ImageUrl))
                        {
                            <img src="~/Assets/@msg.ImageUrl" class="img-fluid rounded mb-2" style="max-height: 200px;" onerror="this.style.display='none';" />
                        }

                        <div style="word-wrap: break-word;">@msg.Content</div>

                        <div class="text-end mt-1" style="font-size: 0.7rem; opacity: @(isMine ? "0.8" : "0.5");">
                            @msg.SentAt.ToString("HH:mm")
                        </div>
                    </div>
                </div>
            }
        }
    </div>

    <div class="p-3 bg-white border-top flex-shrink-0">
        <form id="sendMessageForm" class="d-flex align-items-center gap-2 m-0">
            <input type="hidden" id="activeConversationId" value="@Model.Id" />

            <button type="button" class="btn btn-light rounded-circle d-flex justify-content-center align-items-center" style="width: 44px; height: 44px;" title="Attach Image">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" viewBox="0 0 16 16">
                    <path d="M4.5 3a2.5 2.5 0 0 1 5 0v9a1.5 1.5 0 0 1-3 0V5a.5.5 0 0 1 1 0v7a.5.5 0 0 0 1 0V3a1.5 1.5 0 1 0-3 0v9a2.5 2.5 0 0 0 5 0V5a.5.5 0 0 1 1 0v7a3.5 3.5 0 1 1-7 0z" />
                </svg>
            </button>

            <input type="text" id="messageInput" class="form-control rounded-pill px-3" placeholder="Type a message..." autocomplete="off" required />

            <button type="submit" class="btn btn-primary rounded-circle shadow-sm d-flex justify-content-center align-items-center" style="width: 44px; height: 44px;">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="currentColor" viewBox="0 0 16 16" style="transform: translateX(-1px) translateY(1px);">
                    <path d="M15.964.686a.5.5 0 0 0-.65-.65L.767 5.855H.766l-.452.18a.5.5 0 0 0-.082.887l.41.26.001.002 4.995 3.178 3.178 4.995.002.002.26.41a.5.5 0 0 0 .886-.083zm-1.833 1.89L6.637 10.07l-.215-.338a.5.5 0 0 0-.154-.154l-.338-.215 7.494-7.494 1.178-.471z" />
                </svg>
            </button>
        </form>
    </div>

</div>

<script>
    $('#sendMessageForm').off('submit').on('submit', function (e) {
        e.preventDefault();
        var text = $('#messageInput').val();
        var convoId = $('#activeConversationId').val();

        if(text.trim() === '') return;

        $('#messageInput').val('');

        $.post('@Url.Action("SendMessage", "Chats")', { conversationId: convoId, content: text }, function() {
            loadMessages(convoId);
        });
    });
</script>