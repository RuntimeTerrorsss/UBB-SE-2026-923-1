<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BookingBoardGames.Src.Views.RegisterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BookingBoardGames.Src.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Width="600" Padding="40" Spacing="0" HorizontalAlignment="Center" VerticalAlignment="Center">

            <TextBlock Text="Create an account" FontSize="28" FontWeight="Bold" Margin="0,0,0,10"/>

            <Grid ColumnSpacing="12" Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="4">
                    <TextBlock Text="Username" FontSize="13" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding Username, Mode=TwoWay}" PlaceholderText="Enter username"/>
                    <TextBlock Text="{Binding UsernameError, Mode=OneWay}" Foreground="Red" FontSize="11" Visibility="Collapsed"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Spacing="4">
                    <TextBlock Text="Display Name" FontSize="13" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding DisplayName, Mode=TwoWay}" PlaceholderText="Enter display name"/>
                    <TextBlock Text="{Binding DisplayNameError, Mode=OneWay}" Foreground="Red" FontSize="11" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>


            <StackPanel Spacing="4" Margin="0,0,0,20">
                <TextBlock Text="Email" FontSize="13" FontWeight="SemiBold"/>
                <TextBox Text="{Binding Email, Mode=TwoWay}" PlaceholderText="Enter email"/>
                <TextBlock Text="{Binding EmailError, Mode=OneWay}" Foreground="Red" FontSize="11" Visibility="Collapsed"/>
            </StackPanel>

            <StackPanel Spacing="4" Margin="0,0,0,12">
                <TextBlock Text="Password" FontSize="13" FontWeight="SemiBold"/>
                <PasswordBox Password="{Binding Password, Mode=TwoWay}" PlaceholderText="Min. 6 characters"/>
                <TextBlock Text="{Binding PasswordError, Mode=OneWay}" Foreground="Red" FontSize="11" Visibility="Collapsed"/>
            </StackPanel>

            <StackPanel Spacing="4" Margin="0,0,0,12">
                <TextBlock Text="Confirm Password" FontSize="13" FontWeight="SemiBold"/>
                <PasswordBox Password="{Binding ConfirmPassword, Mode=TwoWay}" PlaceholderText="Repeat your password"/>
                <TextBlock Text="{Binding ConfirmPasswordError, Mode=OneWay}" Foreground="Red" FontSize="11" Visibility="Collapsed"/>
            </StackPanel>


            <Grid ColumnSpacing="12" Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="4">
                    <TextBlock Text="City" FontSize="13" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding City, Mode=TwoWay}" PlaceholderText="e.g. Cluj-Napoca"/>
                    <TextBlock Text="{Binding CityError, Mode=OneWay}" Foreground="Red" FontSize="11" Visibility="Collapsed"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Spacing="4">
                    <TextBlock Text="Country" FontSize="13" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding Country, Mode=TwoWay}" PlaceholderText="e.g. Romania"/>
                    <TextBlock Text="{Binding CountryError, Mode=OneWay}" Foreground="Red" FontSize="11" Visibility="Collapsed"/>
                </StackPanel>
            </Grid>

            <Button Content="Create account" x:Name="RegisterButton" Command="{Binding RegisterCommand}" HorizontalAlignment="Stretch" Margin="0,10,0,0"/>

            <HyperlinkButton Command="{Binding GoToLoginCommand}" Content="Already have an account? Sign in" HorizontalAlignment="Stretch" Margin="0,12,0,0"/>

        </StackPanel>
    </ScrollViewer>
</Page>
